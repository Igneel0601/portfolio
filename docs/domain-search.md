# Domain / alias search

## 🏆 CHOSEN: `vergnyx`
A deliberate fusion of the whole identity arc:
- **ver** — **Ver**ma (the person)
- **gn** — i**gn**eel (the old alias, carried forward, not abandoned)
- **nyx** — **Nyx** / o**nyx** (Greek goddess of night + dark gemstone — the mythic edge)

So `vergnyx` = *Verma + Igneel + Nyx*. Heritage preserved, anime baggage shed.
Reads "verg-nix." Mythic, arcane, grown-up.
SERP **clean** (exact string unowned; only phonetic shadow is the *defunct* B2B
company "VergX" — different spelling, no conflict for a dev brand). Blended ✅,
mythic ✅, clean ✅, pronounceable ✅, no anime/Christian/Islamic/disease baggage ✅.

**Next steps:**
1. Check `vergnyx.dev` / `vergnyx.com` on name.com (confirm free-tier TLD list).
2. Grab the `vergnyx` handle on GitHub + X for the alias→you association.
3. Point domain at Vercel → set `SITE_URL=https://vergnyx.dev` → redeploy
   (canonical/sitemap/OG/JSON-LD all follow `SITE_URL`, no code edits).
4. Add as a Search Console property; 301 the old domain.

---


Hunting a new handle + domain to replace `igneel` (too crowded — collides with
the Fairy Tail anime character + @igneel.art). Live SERP-vetted via web search.

## Criteria
1. **Empty SERP** — near-zero existing results, so the alias resolves to *only me*
   and ranks #1 instantly.
2. **Is me** — anchored on **Vaibhav** (first name), not Verma.
3. **Fire / dragon energy** — keep igneel's vibe (igneel = fire dragon king).
4. **Suggestive of coding, not explicit** — prefer the `.dev` TLD to carry the
   dev signal over literal `byte`/`code`/`forge` in the word.
5. **Free domain via name.com** — ⚠️ GitHub Student Pack free tier has a limited
   TLD list; `.dev` may not be free. Confirm eligible TLDs before locking a TLD.

## Meta-finding
Short "cool" fire/dragon coinages are **all taken** — World of Warcraft
creatures, metal bands, gamer handles, npm packages. The fantasy-name space is
*more* crowded than `igneel`, not less. Only **name-fused** strings stay empty.

## Candidates

### ✅ Clean (empty SERP) — Vaibhav-anchored (preferred)
| Candidate | Reading | Notes |
|---|---|---|
| `vaibhember` | Vaibhav + ember | exact SERP empty; a bit clunky |
| `vaibhignite` | Vaibhav + ignite | exact SERP empty; clunky |
| `vaibhavignis` | Vaibhav + ignis (fire) | empty exact, but long; namesakes own bare "vaibhav" |

### ✅ Clean (empty SERP) — Verma-anchored (Verma now OK too)
| Candidate | Reading | Notes |
|---|---|---|
| `vermignite` ⭐ | **Verma + ignite** | **WINNER** — empty SERP (web + GitHub), fire (ignite), "ignite" suggests spark/launch → coding-suggestive on `.dev`, unmistakably *Verma*. Pronounced "verm-ignite". |
| `ashvyrm` | ash + wyrm (echoes Verma) | near-miss `@AshWyrm` exists |
| `dracoverma` | draco (dragon) + Verma | mostly empty; `dracover` handle close |

### ❌ Taken / crowded (rejected)
igneel (anime), ignyx (fund/pharma), vyrm·vyrma·vyrmaa (WoW/metal band), vyre
(VYRE Network), ignav (flight API), dravyn (athletes), vulkav (npm), pyrev
(pyRevit), vermyr (Warhammer), ignyr (gamers), vermignis (metal label),
vermascale (scaling cos), emberwyrm (WoW), vermapyre (black metal), vermember
(Vermeer), emberbyte·wyrmbyte (companies), drakeforge (Etsy), vaibhavforge
(forging co), vermillyon (vermilion), vermablaze (Verblaze), vaidrake (Vaidrah
streamer), pyrobhav (Pyrobazaar), drakobhav (ayurvedic), vaignis (phonetic
clash w/ "vagueness/vagina"), bhavyr ("behavior"), emberma (Ember Mae author),
vermadrake ("Vera Drake" + GoT dragons), ashverma (ashverma.com consultant —
taken), vermaspark (Verbaspark), vermavyr (metal bands), vermflare (FFXIV).

## ✅✅ CLEAN WINNERS — pyro (Greek fire) + your name
Fusing `pyro`/`pyrois` (Greek *fire*) with Vaibhav/Verma/Vibhu → guaranteed-unique,
**empty SERP**, keeps the fire heritage, no Christian/Islamic roots.

| Candidate | Meaning | SERP | Verdict |
|---|---|---|---|
| `pyrovibhu` ⭐ | pyro (fire) + Vibhu (all-pervading) = **"all-pervading fire"** (Agni's nature) | **empty** | best meaning; Vibhu = user's pick |
| `pyroverma` ⭐ | fire + Verma | **empty** | best sound, easiest to say |
| `vaibhavpyro` | Vaibhav + pyro | empty | clean but clunky to say |

**Order matters: `pyro`-FIRST wins.** Name-first collides or skews bad:
`vermapyro`/`vermapyrois` → Vermapyre (black-metal artist) + "sounds like a disease";
`vaibhpyro` → vampyro/vampire; `vibhupyre`/`vibhupyro` → pyre + vibhuti (cremation
ash), morbid; `pyrovaibhav` → brushes "Pyrova" brand.

**Decision: `pyrovibhu.dev` (meaning) or `pyroverma.dev` (sound).** Check on name.com.

## ⭐⭐ Mythological fire/dragon names (pagan/dharmic only — no Christian/Islamic/Hebrew)
Pattern: adopt a clean mythological fire-name (like igneel was), hidden meaning à la
Iggy = Ignatius = *ignis* (fire). Sources allowed: Norse, Greek, Sanskrit/Hindu,
Slavic, Welsh, Japanese.

| Candidate | Source / meaning | SERP | Verdict |
|---|---|---|---|
| `pyrois` ⭐ | Greek — "**the fiery one**," fire-breathing sun-horse of Helios | clean (only mythology refs, no brand) | **TOP PICK** — elegant, ownable |
| `vasuki` | Sanskrit — **King of the Nagas** (dragon-serpent king); closest to igneel "fire-dragon king" + Indian heritage | busier (namesakes + *Vasuki indicus* fossil + a company) | strong meaning, less clean |
| `aithon` | Greek — "**blazing**," sun-horse + Odysseus's alias | clean-ish ("aethon" = a robotics co; "aithon" freer) | good |
| `vahni` | Sanskrit — "**fire**" (= Agni) | poet + baby-name + SoundCloud | ok, busy |
| ❌ rejected | `shikhin` (=peacock/Buddhist), `pavaka` (med co), `krishanu` (namesakes), `surtr`/`zmaj`/`dreki`/`eldr`/`plamen` (gaming/brand crowded), `inigo`/`azar` (Christian/Persian-Zoroastrian — excluded) | | |

## ⭐ Vaibhav + grown-up fantasy (earlier direction — "retired the dragons")
| Candidate | Reading | Notes |
|---|---|---|
| `vaibhaven` ⭐ | **Vaibhav + haven** | realm/sanctuary = mature fantasy, literally contains "Vaibhav", reads as a place ("welcome to Vaibhaven"). SERP near-clean (minor Flipkart kurta brand + namesakes). **Frontrunner.** |
| `vairune` | Vai + rune | runes = glyphs/ciphers → coding-fantasy overlap, great for a dev; SERP slightly noisy (song "Vairne", god "Varuna") |
| ❌ taken | — | `vaelor` (® streetwear + GoT dragon), `vaeryn` (Pokémon gym leader), `vaether` (Magistream), `vaelune` (Ashes of Creation), `vairex` (fuel-cell co) — all claimed by games/brands |

## (dragon era — superseded) `vermyx`
After ~65 SERP-vetted candidates, `vermyx` wins. Best sound, genuinely dragon,
clearly *Verma*, techy `-yx` ending.

**Reframe that makes it work:** "verm" = *wyrm* — a dragon literally *is* a worm
in Old English (Igneel-the-wyrm energy). So the worm/Verma root is on-theme, not
a liability. The near-twins (`vermyk`/`vyrmex`/`vermix`/`vermyxe`/`vyrmyk`) all
lose: the `verm-` prefix drags in dewormer drugs (Vermox/Vermex/Vermix) or are
ugly to spell. None beats `vermyx`.

**Only caveat:** not a pristine SERP — shares page 1 with "Vermyx, Brood Mother,"
a RuneScape 3 boss (also a wyrm, so thematically fine; a game wiki, not a
competing person/portfolio).

**Action:** check `vermyx.dev` / `vermyx.com` on name.com → register → set
`SITE_URL` in Vercel → site follows (no code edits) → add as GSC property → 301
the old domain.

---

### (superseded) earlier leading contender note: `vermyx`
`Verma` + `-yx` — sleek, techy ending (Onyx/Nginx vibe), and literally a **wyrm**
(dragon), so on-theme. **Caveat:** not an empty SERP — "Vermyx, Brood Mother" is
a **RuneScape 3 boss** that owns page 1. But it's a niche *game wiki* entity, not
a competing person/portfolio, so co-existence is fine for a dev brand. Domain
itself (`vermyx.dev`/`.com`) likely free. Near-twins to vet if a cleaner SERP is
wanted: `vermyk`, `vyrmex`, `vermyxe`, `vermix`.
  - ❌ `veryx` — REJECTED: Veryx Technologies (network-monitoring co, same
    industry), VERYX® (registered trademark, Key Technology sorters), Veryx
    Global. Tech-space collision + a live ® → worse than vermyx.

## Also clean but "kinda tacky" (user's word): `vermignite`
After ~45 SERP-vetted candidates, `vermignite` is the standout: empty SERP
(web **and** GitHub), fire energy (ignite), "ignite" quietly suggests
spark/launch/build → coding-suggestive (pairs perfectly with `.dev`), and it's
unmistakably *Verma*. Pronounced "verm-ignite".

- **Primary:** `vermignite.dev` (if `.dev` is in the name.com free tier)
- **Fallbacks if `.dev` not free:** `vermignite.codes`, `.me`, or whatever the
  free TLD list allows — the word carries it regardless.
- **Backups (if `vermignite` is somehow registered):** `vaibhignite`, `ashvyrm`.

### Next steps
1. Confirm `vermignite.<tld>` availability on name.com (I can't WHOIS live).
2. Grab the matching handle everywhere (GitHub, X) → consistency builds the
   alias→you association (see SEO work in `todo.md` §2).
3. Set `SITE_URL=https://vermignite.dev` in Vercel env → site follows, no code
   edits (canonical/sitemap/OG/JSON-LD all read from `SITE_URL`).
4. Add the new domain as a Search Console property; 301 the old domain.
